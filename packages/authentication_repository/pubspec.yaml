name: authentication_repository

publish: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.6.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cloud_firestore: ^0.16.0+1
  equatable: ^1.2.6
  firebase_auth: ^0.20.1
