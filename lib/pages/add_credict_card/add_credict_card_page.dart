import 'package:flutter/material.dart';

class AddCredictCardPage extends StatelessWidget {
  static const routeName = 'add_credict_card_page';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Add credict card page'),
      ),
    );
  }
}
